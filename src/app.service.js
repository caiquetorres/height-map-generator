/**
 * Service responsible for handling all the business logic related with
 * the base application.
 */
export class AppService {
  /**
   * Method that returns a `ping` string.
   *
   * @returns {'pong'} a string with the value `ping`.
   */
  ping() {
    return 'pong'
  }
}
